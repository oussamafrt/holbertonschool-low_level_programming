#include "main.h"
#include <stdio.h>

/**
 * mul - Entry point
 * @a : character to be verified
 * @b : character to be verified
 * Return: absolute value
 */

int mul(int a, int b)
{
	return (a * b);
}
